schema_version: 1

context:
  name: types-psutil
  version: "7.2.2.20260130"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: 15b0ab69c52841cf9ce3c383e8480c620a4d13d6a8e22b16978ebddac5590950

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation

requirements:
  build:
    - setuptools
  host:
    - python ${{ python_min }}.*
    - setuptools
    - pip
  run:
    - python >=${{ python_min }}

#  todo: 循环依赖
# tests:
#   - files:
#       recipe:
#         - stubtest_allowlist.txt
#         - stubtest_allowlist_linux.txt
#         - stubtest_allowlist_darwin.txt
#         - stubtest_allowlist_win32.txt
#     requirements:
#       run:
#         - mypy
#         - psutil ==${{ version }}
#         - python ${{ python_min }}.*
#     script:
#       - if: linux
#         then: python -m mypy.stubtest psutil  --allowlist stubtest_allowlist.txt --allowlist stubtest_allowlist_linux.txt
#       - if: osx
#         then: python -m mypy.stubtest psutil  --allowlist stubtest_allowlist.txt --allowlist stubtest_allowlist_darwin.txt
#       - if: win32
#         then: python -m mypy.stubtest psutil  --allowlist stubtest_allowlist.txt --allowlist stubtest_allowlist_win32.txt

about:
  summary: Typing stubs for psutil
  license: Apache-2.0 AND MIT
  license_file: LICENSE
  homepage: https://github.com/python/typeshed

extra:
  recipe-maintainers:
    - mr-c

