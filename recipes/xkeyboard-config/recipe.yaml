schema_version: 1

context:
  name: xkeyboard-config
  version: 2.31

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  # url: ../../pkgs/xkeyboard-config-2.31.tar.bz2
  url: http://www.x.org/releases/individual/data/xkeyboard-config/xkeyboard-config-${{ version }}.tar.bz2
  sha256: da44181f2c8828789c720decf7d13acb3c3950e2a040af5132f50f04bb6aada3

build:
  number: 0
  skip: not linux

requirements:
  build:
    - ${{ compiler('c') }}
    - make
    - autoconf
    - pkg-config
    # the following are expected in the build environment for some reason
    - intltool
    - perl-xml-parser
    - libxslt
  host:
    - python >=3
    - xorg-xproto
    - xorg-libx11

tests:
  - script:
      - test -f "${PREFIX}/share/X11/xkb/geometry/hp"

about:
  license: MIT
  license_file: COPYING
  summary: The non-arch keyboard configuration database for X Windows
  description: |
    The goal is to provide the consistent, well-structured, frequently
    released open source of X keyboard configuration data for X Window
    System implementations (free, open source and commercial).
    The project is targeted to XKB-based systems.
  homepage: https://www.freedesktop.org/wiki/Software/XKeyboardConfig/
  repository: https://www.freedesktop.org/wiki/Software/XKeyboardConfig/Development/
  documentation: https://www.freedesktop.org/wiki/Software/XKeyboardConfig/

extra:
  recipe-maintainers:
    - scopatz

